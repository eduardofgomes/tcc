-- --------------------------------------------------------
--
-- Estrutura da tabela `RESERVAS`
--
CREATE TABLE `USUARIO` (
  `SENHA` varchar(45) NOT NULL,
  `ID` INT NOT NULL AUTO_INCREMENT,
  `EMAIL` varchar(45) NOT NULL,
  `TIPO_ID` INT NOT NULL,
  PRIMARY KEY (`ID`))
ENGINE = InnoDB;

CREATE TABLE `CIDADAO` (
  `ID` INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `NOME` VARCHAR(45) NOT NULL,
  `FOTO` MEDIUMBLOB NOT NULL,
  `CPF` VARCHAR(11) NOT NULL,
  `USUARIO_ID` INT NOT NULL,
  CONSTRAINT fk_usu_cid FOREIGN KEY(`USUARIO_ID`) REFERENCES USUARIO (`ID`)
  )
ENGINE = InnoDB;

CREATE  TABLE `QUADRAS` (
  `ID` INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `NUMERO` INT(6) NOT NULL,
  `NOME` VARCHAR(45) NOT NULL,
  `BAIRRO` VARCHAR(45) NOT NULL,
  `LOGRADOURO` VARCHAR(45) NOT NULL
  )
ENGINE = InnoDB;

CREATE TABLE `DIA` (
  `ID` INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `DIA` VARCHAR(45) NOT NULL
)
ENGINE = InnoDB;

CREATE TABLE `HORARIO` (
  `ID` INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `HORARIO` VARCHAR(45) NOT NULL
)
ENGINE = InnoDB;

CREATE TABLE `RESERVAS` (
  `ID` INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
  `HORARIO_ID` INT NOT NULL,
  `DIA_ID` INT NOT NULL,
  `QUADRAS_ID` INT NOT NULL,
  `CIDADAO_ID` INT NOT NULL,
  CONSTRAINT fk_reserva_horario FOREIGN KEY(`HORARIO_ID`) REFERENCES HORARIO (`ID`),
  CONSTRAINT fk_reserva_dia FOREIGN KEY(`DIA_ID`) REFERENCES DIA (`ID`),
  CONSTRAINT fk_reserva_cidadao FOREIGN KEY(`CIDADAO_ID`) REFERENCES CIDADAO (`ID`),
  CONSTRAINT fk_reserva_quadra FOREIGN KEY(`QUADRAS_ID`) REFERENCES QUADRAS (`ID`)
)
ENGINE = InnoDB;
-- --------------------------------------------------------

-- --------------------------------------------------------

--
-- Estrutura da tabela `TIPO_USUARIO`
--

CREATE  TABLE `TIPO` (
  `ID` INT NOT NULL,
  `NOME` VARCHAR(45) NOT NULL,
  PRIMARY KEY (`ID`))
ENGINE = InnoDB;

-- --------------------------------------------------------

--
-- Estrutura da tabela `USUARIO`
--






-- --------------------------------------------------------

-- Dados da tabela `TIPO`

INSERT INTO TIPO (ID, NOME) VALUES (1, "ADM");
INSERT INTO TIPO (ID, NOME) VALUES (2, "Cidadão");
INSERT INTO TIPO (ID, NOME) VALUES (3, "Zelador");

-- --------------------------------------------------------

-- Dados da tabela `QUADRA`

INSERT INTO QUADRAS (ID, NUMERO, NOME, BAIRRO, LOGRADOURO) VALUES (1, "195", "BEIRA RIO", "JARDIM DOS PASSAROS", "AV. SILVIO GASPARINE");
INSERT INTO QUADRAS (ID, NUMERO, NOME, BAIRRO, LOGRADOURO) VALUES (2, "268", "DECAR", "CENTRO", "RUA ÉRICO DE ABREU SODRÉ");
INSERT INTO QUADRAS (ID, NUMERO, NOME, BAIRRO, LOGRADOURO) VALUES (3, "358", "GRÊMIO", "CENTRO", "AV. DA SAUDADE");

-- --------------------------------------------------------

-- Dados da tabela `DIA`

INSERT INTO DIA (ID, DIA) VALUES (1, "DOMINGO");
INSERT INTO DIA (ID, DIA) VALUES (2, "SEGUNDA-FEIRA");
INSERT INTO DIA (ID, DIA) VALUES (3, "TERÇA-FEIRA");
INSERT INTO DIA (ID, DIA) VALUES (4, "QUARTA-FEIRA");
INSERT INTO DIA (ID, DIA) VALUES (5, "QUINTA-FEIRA");
INSERT INTO DIA (ID, DIA) VALUES (6, "SEXTA-FEIRA");
INSERT INTO DIA (ID, DIA) VALUES (7, "SÁBADO");

-- --------------------------------------------------------

-- Dados da tabela `HORARIO`

INSERT INTO HORARIO (ID, HORARIO) VALUES (1, "DAS 08:00 ÁS 10:00");
INSERT INTO HORARIO (ID, HORARIO) VALUES (2, "DAS 10:00 ÁS 12:00");
INSERT INTO HORARIO (ID, HORARIO) VALUES (3, "DAS 14:00 ÁS 16:00");
INSERT INTO HORARIO (ID, HORARIO) VALUES (4, "DAS 16:00 ÁS 18:00");
INSERT INTO HORARIO (ID, HORARIO) VALUES (5, "DAS 18:00 ÁS 20:00");
INSERT INTO HORARIO (ID, HORARIO) VALUES (6, "DAS 20:00 ÁS 22:00");

-- --------------------------------------------------------